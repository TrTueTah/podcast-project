const authConfig = {
    providers: [
        {
            domain: "https://healthy-bluegill-32.clerk.accounts.dev",
            applicationID: "convex",
        },
    ]
};

export default authConfig;